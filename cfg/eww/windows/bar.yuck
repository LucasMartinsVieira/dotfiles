(include "./widgets/bar.yuck")

(
  defwindow bar0
    :monitor 0
    :geometry
    (
      geometry
      :width "100%"
      :height "1"
      :anchor "bottom center"
    )

      ; Wayland
      :stacking "fg"
      :exclusive true
      :focusable false
      :namespace "bar"

      (bar)
)

(
  defwindow bar1
    :monitor 1
    :geometry
    (
      geometry
      :width "100%"
      :height "1"
      :anchor "bottom center"
  )

      ; Wayland
      :stacking "fg"
      :exclusive true
      :focusable false
      :namespace "bar"

      (bar)
)
